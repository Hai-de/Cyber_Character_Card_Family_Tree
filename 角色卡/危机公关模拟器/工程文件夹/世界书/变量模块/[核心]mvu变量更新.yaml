<%_ setLocalVar('initialized_lorebooks.舆论危机公关模拟器[0]', true); _%>
**变量更新**
以下是故事中需要追踪的关键变量，当前状态以这些变量的值为准。
<status_current_variables>
{{get_message_variable::stat_data}}
</status_current_variables>
在所有文本的最后，进行变量更新。
按照以下规则和格式进行输出，来确定变量是否需要更新，莫要遗漏：
```yaml
rule:
  description:
    - You should output the update analysis in the end of the next response, following the variables list defined in `stat_data` section, which shows the status of the previous turn.
    - In context, variable updates are omitted by the system so they are not shown to you, but you should still add it.
    - There are 4 commands can be used to adjust the data.
    - _.set: Used to set a certain simple value (strings, numbers, booleans). It only supports 2 input args, and it doesn't support arrays or objects as inputs.
      _.assign: Used to insert something into an array or object. It supports 2 or 3 input args.
      _.remove: Used to delete something from an array or object. It supports 1 or 2 input args.
      _.add: Used to add a delta to a number. It only supports 2 input args, and only supports modifications to numbers.
    - If you need to assign or remove multiple values, use `_.assign` or `_.remove` multiple times, not in a single command.
    - !!!Please refer to [占位结构] to add or remove objects, but please do not remove [占位结构]!!!
  introduction:
    - You must rethink what variables are defined, and analyze how to update each of them accordingly.
    - For counting variables, change it when the corresponding event occur but don't change it any more during the same event.
    - When a numerical variable changes, check if it crosses any stage threshold and update to the corresponding stage.
  format: |-
    <UpdateVariable>
        <Analysis>$(IN ENGLISH$)
            - calculate time passed: ...
            - decide whether dramatic updates are allowed as it's in a special case or the time passed is more than usual: yes or no
            - list every variable node that is with a comment, and its data type...
            - Check the comment of this variable and analyze whether it satisfies its change conditions. If yes, decide which command should be used. Do not output reason:...
            - Ignore summary related content when evaluate.
            ...
        </Analysis>
        _.set('${path}', ${value});//${reason}
        _.assign('${path}', ${key}?, ${value});//${reason}
        _.remove('${path}', ${key_or_value}?);//${reason}
        _.add('${path}', ${delta});//${reason}
    </UpdateVariable>
  example: |-
    <UpdateVariable>
        <Analysis>
            世界.当前时间: string, Y, set
            世界.公众情绪基调: string, Y, set
            世界.舆情温度: float, Y, add
            沟通渠道: object, Y, assign
            多方行动表.张三: object, Y, assign
            本局模拟挑战词条: array, Y, remove
            ...
        </Analysis>
        _.set('世界.当前时间', '2024-07-16T20:30:00');//时间流逝
        _.set('世界.公众情绪基调', '理性');// 大家相当冷静
        _.add('世界.舆情温度', 2);//舆情信息发酵中...
        _.set('世界.政治活动', '地区选举临近');// 添加政治活动
        _.set('世界.信息传播速度', '正常'); // 这个时代传播信息速度一般
        _.assign('沟通渠道', '微博', {"特性": ["水军众多", "鱼龙混杂"], "成本": "每次发言需要审核，时间成本高。", "状态": "受限"});
        
        _.assign('多方行动表', {"张三": "["2022年5月3日_36%_发布谣言","2022年6月7日_47%_召集公关人员","2022年8月15日_76%_递交辞职通知书"]"});//张三行动列表
        _.remove('本局模拟挑战词条', '你无法穿西装，这可能会影响社交行为');//开始新模拟删除上次选择的挑战词条
        
    </UpdateVariable>
```